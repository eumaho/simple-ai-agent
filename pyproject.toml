
[project]
name = "langchain-vscode-agent-wsl"
version = "0.1.0"
description = "A simple WSL-friendly VS Code Python LangChain agent app."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
